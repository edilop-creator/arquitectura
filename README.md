# 💻 ARQUITECTURA DE COMPUTADORES (Sitio Web Educativo)

Este proyecto es un sitio web educativo simple desarrollado en **PHP** con **CSS puro**, diseñado para el curso de Arquitectura de Computadores. El sitio demuestra la estructura básica de un proyecto web con un menú de navegación, pie de página, y un buscador de contenido.

## 🛠️ Requisitos del Sistema (Dependencias)

Para ejecutar este proyecto en local o en un servidor, se requieren las siguientes tecnologías:

1.  **Servidor Web:** Un servidor que soporte PHP (como **Apache** o **Nginx**).
2.  **Lenguaje de Programación:** **PHP (versión 7.4 o superior)**.

## 🚀 Cómo Iniciar el Proyecto (Instalación)

1.  **Clonar el Repositorio:**
    ```bash
    git clone [https://github.com/edilop-creator/arquitectura.git](https://github.com/edilop-creator/arquitectura.git)
    ```
2.  **Configurar el Servidor:** Coloque la carpeta clonada en el directorio raíz de su servidor web (ej. `/var/www/html/` en XAMPP/LAMP).
3.  **Acceder:** Abra su navegador y acceda a la ruta `http://localhost/arquitectura/index.php`.

## 📜 Estructura del Proyecto

* `index.php`: Página principal y punto de entrada.
* `componentes/`: Contiene el código reutilizable (navegación, footer).
* `css/`: Archivos de estilos.
* `images/`: Recursos visuales del sitio.

---
*Desarrollado por Edilop para el curso de Arquitectura de Computadores.*